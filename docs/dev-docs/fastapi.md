# Fastapi docs

This docs is for my learning and for referencing

### routers

- FastAPI, we make use of `APIRouting` to create a new `router`.
- Once we create a router, we can then make it perform the same was the `@app` and create the required routes
- Once defined, we need to import this router into our `main.py`(or similar) so it can then use this router
